# Automated testing suite for MA-MDP Traffic Light Control
import os
import sys

# Ensure root directory is in python path so tests can easily import modules
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))
